{
  "uri": "http://example.com/profile-schema.json",
  "fileMatch": ["*"],
  "schema": {
    "oneOf": [
      { "$ref": "#/definitions/teamCityConfig" }
    ],
    "definitions": {
      "teamCityConfig": {
        "type": "object",
        "properties": {
          "url": { "type": "string" },
          "guestLogin": { "type": "boolean" }
        },
        "required": ["url", "guestLogin"]
      }
    }
  }
}
